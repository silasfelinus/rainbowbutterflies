name: Rainbow Agent Attention Dashboard Contract

on:
  pull_request:
    branches: [main]
    paths:
      - 'app/pages/dashboard.vue'
      - 'app/pages/agents/**'
      - 'server/api/agents/profiles/**'
      - 'utils/scripts/verifyAgentAttentionDashboard.test.ts'
      - '.github/workflows/agent-attention-dashboard-contract.yml'
  push:
    branches: [main]
    paths:
      - 'app/pages/dashboard.vue'
      - 'app/pages/agents/**'
      - 'server/api/agents/profiles/**'
      - 'utils/scripts/verifyAgentAttentionDashboard.test.ts'
      - '.github/workflows/agent-attention-dashboard-contract.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install dependencies
        run: npm install --include=optional
      - name: Verify agent human-attention dashboard
        run: npx tsx utils/scripts/verifyAgentAttentionDashboard.test.ts
