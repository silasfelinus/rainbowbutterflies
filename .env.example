KIND_ROBOTS_BASE_URL=https://kindrobots.org
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional stable 32+ character cookie-signing secret. If omitted, Rainbow
# uses an in-memory signing key and auth sessions reset when the server restarts.
RAINBOW_SESSION_SECRET=
